apiVersion: v1
kind: Pod
metadata:
  name: roko-secret
spec:
  containers:
  - name: nginx
    image: nginx
    envFrom:
    - configMapRef:
        name: web-api-config
    envFrom:
    - secretRef:
        name: web-api-secrets
  #imagePullSecrets:
  #- name: registry-secret-worker
    
